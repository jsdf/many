#  Tasks

Work through each task below. For each one: verify the current state, make the fix if needed, commit, and check the box.

- [ ] watch the git repo for changes and refresh the branch changes automatically. do this in an efficient way.